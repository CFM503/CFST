name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: cfst-go/go.mod

      - name: Go Version
        working-directory: cfst-go
        run: go version

      - name: Run Unit Tests
        working-directory: cfst-go
        run: go test -v ./...

      - name: Run Race Detector Tests
        working-directory: cfst-go
        run: go test -race ./...

      - name: Run Go Vet
        working-directory: cfst-go
        run: go vet ./...

  build-and-release:
    needs: test
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: cfst-go/go.mod

      - name: Go Version
        working-directory: cfst-go
        run: go version

      - name: Run Windows Unit Tests
        working-directory: cfst-go
        run: go test ./...

      - name: Run Windows Go Vet
        working-directory: cfst-go
        run: go vet ./...

      - name: Build Windows x64 Binary
        working-directory: cfst-go
        env:
          CGO_ENABLED: "0"
          GOOS: windows
          GOARCH: amd64
        run: |
          New-Item -ItemType Directory -Force -Path ..\dist
          go build -ldflags="-s -w" -o ..\dist\CFST-windows-amd64.exe .

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: CFST v2.1.6
          body: |
            Final probe lifecycle hardening.
            - Fixed scheduler/discovery stop channel lifecycle race
            - Hardened in-flight probe result isolation
            - Added real Candidate L3 integration coverage
            - Added lifecycle race coverage
            - Preserved low-bandwidth discovery architecture
            - Windows 11 x64 release
          files: dist/CFST-windows-amd64.exe
          generate_release_notes: false
